name: API Request Every Hour

on:
  schedule:
    - cron: '0 * * * *'  # Every hour
  workflow_dispatch:     # Allow manual runs too

jobs:
  request_job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}  # Ensures token has access to the private repo

    - name: Install Python packages
      run: pip install requests pandas

    - name: Make API request and save CSV
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
      run: python script/request.py

    - name: Commit and Push updated CSV
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add data/stations.csv
        git commit -m "Update stations.csv [bot]" || echo "No changes to commit"
        git push https://x-access-token:${{ secrets.MY_PAT_TOKEN }}@github.com/FedeGiovannetti/Eco_bici_Ciudad_Universitaria.git
